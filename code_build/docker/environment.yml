# Conda environment for running tsdat pipelines

name: tsdat-pipelines
channels:
  - arm-doe
  - conda-forge
dependencies:
  - python=3.10
  - adi_py>=3.19.2
  - pip
  - pip:
      - "-r requirements.txt"
      - boto3==1.*,>=1.26.109
